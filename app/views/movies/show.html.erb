<h1><%= @movie.title %></h1>
<p><%= @movie.creation_year %></p>
<p><%= @movie.genre %></p>
<h2>Actors</h2>
<ul>
    <% @movie.actors_youngest_first.each_with_index do |actor, index| %>
    <div id="actor-<%= index %>">

        <li><%= actor.name %></li>

    </div>
    <% end %>
</ul>